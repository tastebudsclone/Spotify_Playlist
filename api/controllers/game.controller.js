/*
const Scoreboard = require('../models/scoreboard.model');
const createError = require('http-errors');
const {example, getRandomTracks} = require('../config/spotify.config');



// llamar a example que es la que autentica las credenciales de cliente o no hace falta (?)

module.exports.getRandomSong = async (req, res, next) => {
//  const randomTrack = await //calltheSpotifyConfigFnc (req.body);
};

module.exports.compareGuess = (req, res, next) => {
  const userGuess = 
  const guess =
// hacer un if con la comparacion de strings, devolver 1 si acierta o 0.
};

module.exports.updateScore = (req, res, next) => {
  const user = User.findOne(req.user.id);
  user.score += score;

  req.user
  .save()
  .then(next)
  .catch(next);
};

*/